[build]
  base = "client"
  command = "npm run build"
  publish = "dist"          # <— NOT client/dist

[functions]
  directory = "netlify/functions"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
